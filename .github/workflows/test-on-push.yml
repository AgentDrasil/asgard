name: Test on Push

on:
  push:
    branches: [ main ]

env:
  GO_VERSION: 1.26
  GOIMPORTS_VERSION: latest
  GOLANGCI_LINT_VERSION: latest
  NODE_VERSION: 26
  PNPM_VERSION: 11.3.0

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: ${{ env.GO_VERSION }}

    - name: Install goimports
      run: go install golang.org/x/tools/cmd/goimports@${{ env.GOIMPORTS_VERSION }}

    - name: Format check
      run: |
        test -z "$($(go env GOPATH)/bin/goimports -l .)"

    - name: Lint check
      uses: golangci/golangci-lint-action@v9
      with:
        version: ${{ env.GOLANGCI_LINT_VERSION }}
        working-directory: .
        args: ./backend/... ./backend/agentwrapper/... ./backend/fakebash/... ./backend/agystatusline/...

    - name: Run tests
      run: |
        go test -v ./backend/... ./backend/agentwrapper/... ./backend/fakebash/... ./backend/agystatusline/...

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webui
    steps:
    - uses: actions/checkout@v7

    - name: Set up pnpm
      uses: pnpm/setup@v2
      with:
        version: ${{ env.PNPM_VERSION }}
        runtime: node@${{ env.NODE_VERSION }}

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: Lint check
      run: pnpm run lint

    - name: Format check
      run: pnpm run fmt:check

    - name: Run tests
      run: pnpm run test

    - name: Build
      run: pnpm run build

services:
  asgard:
    container_name: asgard
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: runner
    volumes:
      - ./tmp/docker/home:/home/user
    privileged: true
    ports:
      - "8080:8080"
    command: [ "asgard" ]
